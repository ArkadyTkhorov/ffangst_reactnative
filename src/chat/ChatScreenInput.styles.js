/** @module src/chat/ChantScreenInput.styles */

import { StyleSheet } from 'react-native';

/**
 * Stylesheet for ChantScreenInput.
 * @type {StyleSheet}
 */
const styles = StyleSheet.create({
  input: {
    flex: 1,
    fontSize: 16,
    maxHeight: '100%'
  }
});

export default styles;

